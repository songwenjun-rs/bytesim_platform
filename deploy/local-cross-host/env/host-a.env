# Host A — edge nginx
# BFF_UPSTREAM is set in the compose file `environment:` block (nginx
# resolves it via envsubst at container start), kept here for reference.
# BFF_UPSTREAM=host.docker.internal:18080
