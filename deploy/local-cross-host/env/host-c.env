# Host C — data tier
# Postgres is now a STANDALONE service (docker-compose.postgres.yml).
# data_svc reaches it via the host machine, same as any other "host".
PG_DSN=postgres://bytesim:bytesim@host.docker.internal:15432/bytesim?sslmode=disable
LISTEN_ADDR=:8081
