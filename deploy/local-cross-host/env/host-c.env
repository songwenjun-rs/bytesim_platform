# Host C — data tier
# postgres lives on the same bridge, so data_svc uses the service-name
# DNS for its DB connection. Other hosts go through host.docker.internal.
PG_DSN=postgres://bytesim:bytesim@postgres:5432/bytesim?sslmode=disable
LISTEN_ADDR=:8081
