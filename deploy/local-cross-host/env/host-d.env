# Host D — engine tier (shared base env; each service overrides specifics
# via the docker-compose.host-d.yml `environment:` block).
# All service-specific URLs are set per-container in the compose file.
