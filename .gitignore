# Python ────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python
.eggs/
.pytest_cache/
.coverage
.coverage.*
# Local pytest helper scripts (used to invoke pytest inside docker python:3.12).
.run-pytest.sh
.run-pytest-with-pg.sh
htmlcov/
tests/_cov_html_python/
.tox/
.venv*/
venv/
env/
.python-version
*.egg

# Node / web ────────────────────────────────────────────────────────
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Go ─────────────────────────────────────────────────────────────────
*.test
*.out

# Editors / OS ──────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# CI artifact downloads (debugging) ─────────────────────────────────
e2e-logs*.zip
*.tar.gz


# Secrets — never commit ────────────────────────────────────────────
.env
.env.local
*.pem
*.key
secrets/

# Claude Code local config / session state (per-user, not for repo) ─
.claude/

# Phase 3 split — the 9 services live under bff/, dashboard/, service/X_svc/,
# engine_contracts/ as git submodules. Each has its own gitlink; platform repo
# tracks the pinned commit + submodule URL via .gitmodules.
/output/
